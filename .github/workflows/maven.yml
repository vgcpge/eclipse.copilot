# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
    secrets:
      CODE_SIGNING_KEYSTORE:
        description: 'Base 64 encoded keystore with code-signing private key. Alias: codesign, password: changeit'
        required: false
        type: string
  workflow_call:
    secrets:
      CODE_SIGNING_KEYSTORE:
        description: 'Base 64 encoded keystore with code-signing private key. Alias: codesign, password: changeit'
        required: false

jobs:
  build:

    runs-on: ubuntu-latest
    env:
      CODE_SIGNING_KEYSTORE: ${{ secrets.CODE_SIGNING_KEYSTORE }}
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Read secrets
      run: |
        if [ -n "${CODE_SIGNING_KEYSTORE}" ] ; then
          base64 --decode >signing.keystore <<< "${CODE_SIGNING_KEYSTORE}" || exit 1
          echo SIGN_ARGUMENT=-Dcode-signing-keystore=signing.keystore>>"$GITHUB_OUTPUT"
        fi
    - name: Build with Tycho
      run: mvn --batch-mode --threads 2.0C ${SIGN_ARGUMENT} clean verify --file pom.xml
    - name: Upload repository
      uses: actions/upload-artifact@v3
      with:
        name: repository
        path: 'repository/target/repository/'

    # Optional: Uploads the full dependency graph to GitHub to improve the quality of Dependabot alerts this repository can receive
    #- name: Update dependency graph
    #  uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6
